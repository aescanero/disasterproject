all:
  children:
    vms:
      hosts:
        docker:
          memory: 1024
          vcpus: 1
          vm_ip: "192.168.8.2"
          linux_flavor: "debian"
          container_engine: "docker"
        podman:
          memory: 1024
          vcpus: 1
          vm_ip: "192.168.8.3"
          linux_flavor: "debian"
          container_engine: "podman"
      vars:
        network_name: demo-network
        network: "192.168.8"

