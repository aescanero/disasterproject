all:
  children:
    vms:
      hosts:
        vm1:
          memory: 1024
          vcpus: 1
          vm_ip: "192.168.8.2"
          linux_flavor: "debian"
      vars:
        network_name: demo-network
        network: "192.168.8"
