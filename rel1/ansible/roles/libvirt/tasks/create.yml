- name: create_vm
  virt:
    command: define
    name: "{{ name }}"
    uri: "{{ libvirt_uri }}"
    xml: "{{ lookup('template', 'vm.xml.j2') }}"

- name: start_vm
  virt:
    command: start
    name: "{{ name }}"
    uri: "{{ libvirt_uri }}"

